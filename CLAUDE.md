# AI 開発アシスタント向けルール

## 必読ドキュメント

**このファイルを読む前に、必ず `README.md` を読んでください。**

README.md には以下が記載されています：

- プロジェクト概要・背景・目的
- アーキテクチャ方針（3 層構造、責務の配置）
- 技術スタック
- ディレクトリ構成
- 開発ルール（マイグレーション、API Routes、リアルタイム）
- 開発フロー

## 厳守事項

### ❌ 禁止行為

1. **Claude Code でのファイル変更は全面禁止**

   - 理由：勉強目的のプロジェクトであり、大規模変更は収集がつかなくなるため
   - Claude Code の役割：コードレビュー、バグ調査、設計相談のみ

2. **勝手な設計変更**

   - README.md に記載のアーキテクチャ方針に従う
   - 変更提案は必ず理由を添えて確認を求める

3. **過度な抽象化・複雑化**
   - MVP 優先、シンプル第一
   - 「将来的に必要になるかも」は NG

### ✅ 推奨行動

1. **レビュー時**

   - README.md のルールに沿っているかチェック
   - アーキテクチャの責務分離が正しいか確認
   - 型安全性、エラーハンドリング、RLS を重点的に

2. **相談時**

   - 複数の選択肢を提示
   - それぞれのメリット・デメリットを説明
   - 「なぜその設計がベターか」理由を添える

3. **コード例を出すとき**
   - 実践的で、そのまま使えるものを
   - README.md の命名規則・ディレクトリ構成に従う
   - TypeScript の型定義を必ず含める

## レビュー観点

### 最優先チェック項目

- [ ] README.md のアーキテクチャ方針に従っているか
- [ ] RLS ポリシーは適切か（全テーブル有効化、auth.uid() 使用）
- [ ] エラーハンドリングは十分か
- [ ] 型定義は正確か（`any` を使っていないか）
- [ ] マイグレーションファイルは冪等か（DROP IF EXISTS → CREATE）

### 設計レビュー時の判断基準

**「この処理はどこに置くべきか？」**

| 処理内容             | 配置先                | 理由                               |
| -------------------- | --------------------- | ---------------------------------- |
| データ整合性制約     | Supabase（DB 層）     | NOT NULL, UNIQUE, CHECK, FK で保証 |
| 認証・認可           | Supabase（RLS）       | auth.uid() でユーザー識別          |
| **全てのCRUD**       | Next.js API Routes    | ロジック配置の一貫性確保           |
| ビジネスロジック     | Next.js API Routes    | 「18 歳未満は機能制限」など        |
| バリデーション       | Next.js API Routes    | Zod で定義                         |
| 外部 API 連携        | Next.js API Routes    | アニメ情報 API など                |
| Realtime 購読        | フロントエンド        | カスタムフック化必須（購読のみ）   |

**注意**: フロントエンドからSupabaseへの直接アクセスは「Realtime購読のみ」に限定。データの読み書きは全てAPI Routes経由。

## 回答スタイル

### やってほしいこと

- わからないことは「わからない」と言う（ウソ禁止）
- まず自分の考えを述べ、その後私の考えと照合
- 代替案がある場合は複数提示
- 過剰な説明は不要、必要なら聞く

### やってほしくないこと

- 無条件の肯定（「いいですね！」だけの回答）
- 曖昧な回答（「〜かもしれません」の連続）
- README.md を無視した提案

## トラブルシューティング

よくある問題と対処法（README.md から抜粋）：

| 問題                 | 対処法                                        |
| -------------------- | --------------------------------------------- |
| マイグレーション失敗 | `supabase db reset` で初期化                  |
| Realtime が動かない  | テーブルの Realtime 有効化 + RLS ポリシー確認 |
| 型エラー             | `npm run gen:types` で型を再生成              |

## 補足

- コミット前は必ず `npm run lint` と `npm run type-check`
- マイグレーションは `supabase db reset` で検証してからコミット
